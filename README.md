# ccakes/homebrew-tap

```sh
brew tap ccakes/tap
brew install workbench       # TUI process orchestrator (bench)
brew install --cask elo      # Numi-compatible notepad calculator
```

Formulas auto-bump daily from upstream GitHub releases via `scripts/bump.sh`.
