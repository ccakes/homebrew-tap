name: Audit

on:
  pull_request:
  push:
    branches: [main]

jobs:
  audit:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: brew audit
        run: brew audit --strict Formula/workbench.rb Casks/elo.rb
